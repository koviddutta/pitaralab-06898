# Frontend env (only public/anon keys here)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Feature Flags (set to "on" or "off")
# Defaults to "on" if not specified
VITE_FLAG_AI=on
VITE_FLAG_SCIENCE_PANEL=on
VITE_FLAG_PRODUCTION_MODE=on

# Optional analytics (leave empty if not used)
VITE_ANALYTICS_WRITE_KEY=
